# リリース前チェック

自動で確認できる分は、`.github/workflows/checks.yml` が push と PR のたびに実行する。
手元で先に確かめる場合は、下記2つを直接動かす。

## キャッシュ回避クエリ

`app.js` や `styles.css` を変更したら、`index.html` と `styles.css` の `?v=` を内容ハッシュへ揃える。アトラスPNGを差し替えたときも同じコマンドでよい。

- `node scripts/cache-version.mjs` を実行し、変更後の `index.html`・`styles.css`・`sw.js` を一緒にコミットする
- `node scripts/cache-version.mjs --check` が成功すること（ずれていれば終了コード1）
- 番号を手で上げる運用は廃止した。上げ忘れると更新が既存ユーザーへ届かないため
- `vendor/` 配下は同梱ライブラリのバージョン表記なので、このスクリプトの対象外

## 配信用WebP

- `node scripts/build-webp.mjs --check` が成功すること（PNGを差し替えてWebPを作り直し忘れていないか）
- アトラスのPNGを差し替えたら `node scripts/build-webp.mjs` → `node scripts/cache-version.mjs` の順に実行し、
  生成物を一緒にコミットする
- 食材と料理のイラストが、冷蔵庫・レシピ・買い物・食材追加の各画面で欠けずに出ること

## 食材の初期単位

- `node scripts/check-initial-units.mjs` が成功すること
- イラストから食材を追加し、初期数量と単位が一般的な買い方になっているか実機で確認する
- 特に、なすが「1袋」ではなく「1本」で追加されることを確認する
- レシート認識候補、買い物リスト、在庫、レシピの必要量で単位が一致することを確認する
- 袋・パック・本数の地域差や商品差が大きい食材は、初期値のまま確定せず数量操作で直せることを確認する

このチェックは新しい食材・レシピ・イラストを追加したリリースごとに実施する。

## 二人で共有する

- 招待リンクを開いただけでは同期せず、参加前の確認画面が出ること
- 既存データがある端末では、在庫・買い物・履歴の件数が表示されること
- 「共有の内容に入れ替える」では、端末側の既存内容を共有先へ送らないこと
- 「今の内容も合わせる」では、端末側の既存内容を送って統合すること
- 空の端末では統合の選択肢を出さず、そのまま参加できること
- 存在しないIDや通信失敗時に、参加前の在庫・買い物・履歴・棚・共有状態へ戻ること
- キャンセル後は共有していない状態のままであること
- 端末名を変更して再読み込みしても、その端末だけに同じ名前が残ること
- 端末Aで変更した在庫・買い物・履歴に、変更時刻と端末Aの名前が表示されること
- 同期後の端末Bにも同じ変更時刻と端末Aの名前が表示されること
- 旧版から届いた変更は、時刻を表示しつつ端末名を「端末不明」と扱えること
- 共有中はトップ上部に「同期」ボタンが表示され、共有していない端末では表示されないこと
- 在庫・買い物・履歴・棚の数を変更すると「未同期」になり、その後自動で同期されること
- 続けて複数回変更した場合は、最後の操作から少し待って同期が1回にまとまること
- 自動同期に失敗した場合は「未同期」が残り、トップ上部のボタンで再試行できること
- 食材を使い切った場合と廃棄した場合が、別の種類として履歴へ残ること
- 在庫の数量・単位・保管場所を修正すると、修正前と修正後が履歴へ残ること
- 調理前に「ついでに使う食材」を追加し、その量も在庫から減ること
- 調理履歴の「使った食材を修正」で、食材の追加・削除・数量変更の差分だけ在庫へ反映されること
- 調理履歴を修正すると自動同期され、もう一方の端末でも修正後の材料と数量になること
- 旧版の調理履歴が引き続き「調理」として表示・取り消しできること

## iPad mini表示

- 縦向き（744 × 1133相当）で、レシピ・在庫・履歴が2列表示になり、横スクロールが出ないこと
- 横向き（1133 × 744相当）で、左側ナビゲーションとレシピ3列表示へ切り替わること
- 横向きの買い物画面で、追加候補と買い物リストを左右で同時に確認できること
- 食材追加・調理確認・調理履歴修正のダイアログが画面中央に収まり、保存ボタンまで操作できること
- 390 × 844相当では従来の1列表示と下部ナビゲーションが維持されること
- 長い画面を最下段までスクロールしても、下部メニューと横向きの左メニューが1pxも移動しないこと
- 冷蔵庫／パントリーが画面内に収まる場合は縦スクロール量が0になり、棚を増やして収まらない場合はスクロールが戻ること

## 賞味期限

- 食材の新規登録で賞味期限を空欄のまま保存できること
- 食材の新規登録で日付欄をタップするとカレンダーが開き、賞味期限を保存できること
- 冷蔵庫または在庫管理から食材をタップし、賞味期限を追加・変更・解除できること
- 期限切れ・今日まで・3日以内の食材だけが冷蔵庫上部のお知らせに出ること
- お知らせの食材をタップすると、その食材の編集画面が開くこと
- 賞味期限の変更が履歴に残り、共有冷蔵庫の別端末にも同期されること

## 冷蔵庫と在庫の行き来（2026-09-04 のタブ統合）

- 下部ナビが4項目（冷蔵庫・レシピ・買い物・履歴）で、「在庫」の項目が無いこと
- 冷蔵庫の絵の見出し右の「一覧で直す」で一覧へ、一覧の「冷蔵庫の絵へ」で絵へ戻れること
- どちらのモードでもナビの現在地が「冷蔵庫」であること
- ホーム画面アイコンの長押しから「在庫」（`#stock`）を開くと一覧が出て、
  ナビの現在地が「冷蔵庫」になること。`#recipes`・`#shopping` も従来どおりであること

## 冷蔵庫の絵

- 冷蔵室が上、冷凍室が下に並ぶこと（国内の家庭用冷蔵庫の一般的な配置）
- 食材が上の棚から詰まること（3品を3段の冷蔵室へ入れて、1段目に3品入ること）
- 棚の列数が中身で変わること（4品まで4列、5品で5列、6品で6列。折り返さず1行に収まる）
- 「＋」が列数に数えられず、格子の右端の細い列に出ること（36×44px）
- 絵の下に食材の名前が1行で出ること。6文字以上の名前は略称（`FRIDGE_SHORT_NAMES`）で出ること
- 5列以上に詰まったときだけ名前が11pxになり、4列では12pxであること
- 700px以上で筐体が広がり（640px）、6列でも名前が省略されずに出ること
- 段数の −／＋ が右端の丸ボタンで、押せる大きさが44px以上あること
- 700px以上で食材が6列に広がること

## 食材追加モーダル

- 冷蔵庫トップに「一覧から一度に追加する」が表示されないこと
- 在庫の一覧の「＋ 食材を追加」を開くと、まず「1. 種類を選ぶ」が見えること
- 「レシートからまとめて追加」と「一覧から一度に追加する」が、種類のカードより**下**に同じ場所で並ぶこと
- 「2. 食材を選ぶ」へ進むと、その2つが表示されないこと
- 既存食材の編集時は、2つの一括追加ボタンが表示されないこと
- 「一覧から一度に追加する」を押すとモーダルが閉じ、売り場ごとの一括追加画面へ移動すること
- モーダルに「在庫を整える」「売り場を探すように選べます」が表示されないこと

## 買い物リスト

- 「店内でチェック」のカードに、食材の絵・名前・数量が常に出ていること
- 買うものが1つも無いとき、「今あるものに合う」が開いた状態で出ること。
  自分で閉じたら、そのセッション中は開き直さないこと
- カードをタップすると詳細が下へ開き、「購入済みにする」と「削除」が押せること

## 削除の3種類

- 在庫の一覧の行に並ぶのが「まだある」「先に使う」「使い切った」の3つで、
  「完全削除」が行に無いこと
- 食材をタップした編集画面に「使い切った」「捨てた」が並び、
  いちばん下に小さく「履歴に残さず消す」があること
- 「使い切った」「捨てた」は履歴に残り、トーストの「戻す」で取り消せること
- 「履歴に残さず消す」は履歴に残らず、トーストの「戻す」で戻せること

## 作る前の確認

- 在庫がある「あるとより良い」だけが開いて並び、在庫が無いものは
  「いま無い『あるとより良い』 ◯品」に畳まれていること
- 「ついでに使う食材」が畳まれていて、押すと食材の選択と量の入力が出ること

## レシピの外部検索

- Google検索が文字の「G」ではなく、虫眼鏡アイコンで表示されること
- YouTube検索が、赤い角丸の動画枠と白い再生三角のアイコンで表示されること
- どちらも44px前後の押せる領域を保ち、Google検索・YouTube検索のリンク先が変わっていないこと

## 栄養表示の説明

- 設定の「カロリーとPFCを表示する」の隣に「？」ボタンがあること
- 説明は初期状態では隠れ、「？」を押したときだけ「表示される数値は、食品ごとの…」と表示されること
- 栄養表示スイッチがOFFでも説明を開閉でき、説明の開閉ではスイッチの状態が変わらないこと
- 設定画面を閉じて開き直すと、説明が畳まれたコンパクトな状態へ戻ること
