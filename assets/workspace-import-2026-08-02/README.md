# Workspace import — 2026-08-02

ワークスペース直下の旧 `assets/` に残っていた、冷蔵庫アプリ用の食材画像候補を保全したもの。

- `atlas-src/23/`: 12枚。本体の同名12枚とはSHA-256が異なるため、上書きせず候補として保持。
- `atlas-src/23-keyed/`: 12枚。本体側に同名ファイルが無かったため、採用前の候補として保持。
- 移動前の26ファイル（`.DS_Store` 2件を含む）は、移動後も全件SHA-256一致。

採用判断までは `scripts/build-atlas.mjs` の通常入力へ混ぜない。比較後に採用する場合も、現在の `assets/atlas-src/23/` を先にバックアップする。
