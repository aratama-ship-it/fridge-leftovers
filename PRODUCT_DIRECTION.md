# 冷蔵庫アプリ：プロダクト方針メモ

## 文書の位置づけ

- 状態：対話を通じて更新中。決まったことはここへ追記する
- 用途：プロダクトの核、決定事項、比較案を残す
- 実装状況：節ごとに「実装済み」「未実装」「部分実装」を明記する
- 公開中のアプリとの関係：本書は公開版 v0.9.0 の実装状況を反映している

初版は2026-07-23、レシピ10件の初期プロトタイプを公開していた時点で書いた。
当時は「公開中のアプリへの反映：まだ行わない」と宣言していたが、その後の開発が
本書を参照できない別ブランチで進んだため、一部の項目だけが実装された状態になった。
2026-07-25に実装状況と突き合わせ、現役の方針書として更新している。

## プロダクトの核

このアプリの価値は、レシピの網羅性そのものではない。
収録件数は多くしてよいが、1件あたりの記述は簡易にとどめる（→[レシピの収録方針](#レシピの収録方針)）。

料理に強くこだわらない日に、

- 帰り道で「冷蔵庫に何があったか」を思い出す
- スーパーで必要な買い足しを判断する
- 帰宅後、家にあるもので簡単に一食を決める

という一連の流れを軽くする。

中心的な価値は、細かな在庫管理や本格的な献立作成ではなく、**記憶と判断の負担を減らし、十分な一食へ早く着地させること**。

## 主な利用の流れ

1. 帰り道に、冷蔵庫の中身を確認する
2. 必要ならスーパーで買い足す
3. 帰宅後、今あるもので料理を決める

この中では「1. 帰り道の確認」が最も重要な入口。

## 変更しない表現の核

冷蔵庫の中へ食材イラストが入っているUIを、プロダクト固有の中心表現とする。

- 実在する食材を、二次元でポップに表現する
- 文字の在庫表を読ませるのではなく、見た瞬間に中身を思い出せる
- 食材イラストは装飾ではなく、在庫確認と操作の中心
- 食材が増えるほど、実際の冷蔵庫を育てるような感覚を作る

**実装状況：実装済み**（203種の食材イラスト、冷蔵室・冷凍室・パントリー棚、長押しでの棚移動、子どものイラストへのドラッグで使い切り。未対応の食材は頭文字で表示）

## 在庫の見せ方

ユーザーへ見せる残量は、基本的に次の3状態とする。

- ある
- 少ない
- ない

具体的なグラム数や個数をトップ画面へ常時表示しない。
「少ない」は、イラストの大きさ、数、棚での位置などを使って視覚的に表す案を検討する。

システム内部では、料理履歴から推定した数量を持つことができる。

**実装状況：実装済み（2026-07-31）**

- トップ画面の残量の帯は3段階になった（まだあります＝満量の緑／少なめ＝半分の琥珀／残りわずか＝1/4の赤）。境目は翌日の確認と同じ 0.5 / 0.25
- 読み上げも「残量約◯パーセント」をやめ、状態の言葉にした
- 「ない」は使い切りとして画面から消える（削除の履歴に入る）
- 未実装のまま：「少ない」をイラストの大きさ・数・位置で表す案（帯で足りているか、使用感を見てから）

## 在庫更新の流れ

1. ユーザーが提案された料理を「作った」と記録する
2. レシピ上の使用量を、仮の消費量として在庫へ反映する
3. 翌日、「昨日使った食材はこれで合っていますか？」と確認する
4. 実際の使用量が違う場合は、プラス・マイナスのタップで補正する

例：レシピ上は卵1個だが、実際には2個使った場合、翌日の確認で2個へ直す。

表向きの操作は簡単に保ちながら、裏側の推定在庫を少しずつ現実へ合わせる。

**実装状況：実装済み（2026-07-26）**

「これを作る」で引くのはレシピ上の分量なので、引いたあとの残りは推定になる
（在庫に `quantityConfidence = 推定` を付ける）。**昨日以前に作った分**で、まだ
実物と突き合わせていない食材があれば、冷蔵庫画面の上に確認カードを出す。

聞き方は**3状態**にした（「ある」「少ない」「ない」）。グラム数を思い出させるのは
負担が大きく、核に反する。数量は内部で保ったまま、答えた状態の代表値へ寄せる。
境目は残量ゲージと同じ（0.5 / 0.25）にしてあるので、押した直後の見た目と
食い違わない。

- 「ある」→ 残量の帯が「ある」に入るところまで上げる。**今の量より下げはしない**
- 「少ない」→ 少なめの帯に入れる
- 「ない」→ 使い切りにする。逆に、使い切ったことになっているものを「ある」で戻せる
- どれを答えても確認済みに戻るので、同じ食材を繰り返し聞かない

一度に聞くのは**4品まで**。使い切ったことになっているものと、残りが少ないものを
先に出す。「あとで」を押した日はもう出さず、翌日また出す。

**実装中に見つけて直した点**：調理を取り消しても確信度が推定のまま残り、作って
いないのに翌日の確認へ出てきていた。取り消しで確信度も元へ戻すようにした。

## レシピの収録方針

**決定（2026-07-25）：レシピは多く収録する。ただし1件あたりの記述は簡易的な文章にとどめる。**

- 件数は増やしてよい。「今ある食材では作れるものが1つも無い」状態を避けることを優先する
- 1件あたりに持つのは、料理名・所要時間の目安・3手順程度の簡易文
- 詳しい手順、火加減、コツ、分量の根拠はアプリの担当範囲にしない。必要な人は各レシピの外部検索リンクからGoogle・YouTubeへ移動する
- 本格的なレシピサイトとしての完成度は競わない。「今あるもので一食に着地する」ために足りる最小限の説明に保つ

初版では「レシピを大量に収録することを主な価値にしない」と書いていたが、
収録件数を絞る意味に読めたため、上記のとおり「件数は多く、記述は簡易に」へ改めた。

### 簡易にするのは調理の説明であって、材料データではない

材料と分量は、在庫との突き合わせ（不足判定・調理後の在庫減算・買い足し推薦）に不可欠な内部データなので、構造化して持ち続ける。ここを簡略化すると機能が成立しない。

- 構造として持つ：食材ID、分量、単位、「最低限必要」と「あるとより良い」の区別
- 簡易にとどめる：ユーザーへ見せる調理手順の文章

**実装状況：実装済み**（77件。各件が3手順の簡易文と外部検索リンクを持つ。材料は構造化データとして保持）

## 料理提案の構造

料理候補は3つ用意するが、同じ強さで並べない。

- 「今日のおすすめ」1件を大きく表示
- 「ほかの候補」2件を控えめに表示

通常状態で見せる情報：

- 完成イラスト
- 料理名
- 所要時間
- 冷蔵庫から使う食材
- 買い足しが必要か

「簡単な作り方」をタップすると、アコーディオンで3手順程度を表示する。
詳しい作り方が必要な場合は、外部レシピサイトへ誘導する。

レシピサイトとの連携は有力な将来展開だが、このアプリの中心的な売りとはしない。

**実装状況：部分実装**

- 実装済み：おすすめ1件と控えめな2件の出し分け、料理名、所要時間、使う食材、不足食材の表示、3手順アコーディオン、外部誘導（Google・YouTubeの検索リンク）
- 完成イラスト：**表示側は実装済み（2026-07-31）**。おすすめは108px・ほかの候補は76pxで
  料理名の左に出る。絵が無いレシピは従来どおりの表示（RECIPE_ILLUSTRATIONS に
  無いだけで壊れない）。絵はCodexが生成中で、r01〜r05（60品）を組み込み済み。
  残り17品はシートが届き次第この表へ足す

## 栄養情報

カロリーとPFCバランスはオプション機能とする。

- 初期状態では料理選びを複雑にしない
- 設定でオン・オフを切り替えられる
- オンの場合も、数値は推定値・目安であることを明示する
- 食への興味を持つきっかけとして扱い、健康管理を強制しない

**実装状況：実装済み（2026-07-26）**

ホーム（冷蔵庫画面）右上の歯車から設定を開き、「カロリーとPFCを表示する」で切り替える。初期値はオフ。オフのときはレシピ一覧と調理確認画面の両方から数値が消える。オンにすると、目安であることの注記が設定画面に現れる。設定は `fridge-leftovers-settings-v1` として端末内に保存する。

歯車はホーム専用（他の画面では隠す）。もともとここに置いていた「＋食材」は、在庫画面の「＋食材を追加」と冷蔵庫の各棚の「＋」に入口があるため取り下げた。

## 昼食を夕食推薦へ反映する

一般的な料理名を簡単に入力する。

入力例：

- ラーメン
- カレー
- 牛丼
- パスタ
- 寿司

量、店名、細かな具材は入力させない。
料理名から大まかな食事傾向を推定し、夕食で同じ系統が続きすぎないよう推薦順位へ反映する。

入力の挙動：

- 未入力なら起動時に聞く
- スキップした場合、1時間以内は再表示しない
- 1時間以上経過後の起動時にもう一度聞く
- 一度入力した日は再表示しない
- 翌日にリセットする
- 設定で機能自体をオフにできる

**実装状況：未実装**（入力の口も、推薦順位への反映も無い）

## 初回の冷蔵庫登録

### 第一案：カテゴリーごとに食材を選ぶ

一般的な食材をカテゴリーごとに順番に表示し、イラストをタップして冷蔵庫へ入れる。

カテゴリー例：

1. 野菜
2. 肉・魚
3. 卵・乳製品
4. 主食
5. 常温品・調味料

一画面へ多数の食材を詰め込まず、対象を少しずつ確認できるため、探すストレスを抑えやすい。
現段階ではこの方式を第一案とする。

#### 採用する初回登録体験

冷蔵庫のイラストを画面に出したまま、カテゴリーごとの食材トレイを順番に表示する。

1. 「野菜」の食材イラストが画面下から出てくる
2. 食材をタップすると、冷蔵庫の野菜室へ入る
3. 選択した食材が冷蔵庫内に残り、登録結果をその場で確認できる
4. 次に「肉・魚」「卵・乳製品」「主食」などへ進む
5. 最後に、自分の冷蔵庫が完成した状態を見せる

チェックリストへ印を付けるだけではなく、**自分の冷蔵庫へ実際に食材を詰めていく感覚**を作る。
食材が棚へ移動する短い動きと、入った直後の反応を気持ちよく仕上げる。

登録の進め方は、次の二つから選べるようにする。

- **ここまでで使い始める**：途中のカテゴリーまで登録し、すぐ冷蔵庫画面へ進む
- **全部確認して完成させる**：すべてのカテゴリーを順番に確認する

各カテゴリーはスキップ可能にし、途中で始めた人には後から続きを案内できるようにする。
すべて確認したい人には進行状況を見せ、最後まで埋めること自体に小さな達成感を作る。
どちらを選んでも「登録が不完全」という否定的な見せ方はしない。

**実装状況：実装済み（2026-07-26）。ただし採ったのはこの案ではない**

サンプルの5品は、新しく使い始める人には入らなくなった。代わりに下記
「初回オンボーディングの設計」の入口（主役を選ぶ→候補→分かれ目の食材）を出す。
カテゴリー式で全カテゴリーを回る体験は作っていない（格下げしたまま）。

### 比較案：定番20品を先に表示する

卵、牛乳、豆腐、キャベツ、玉ねぎなど、保有率が高そうな食材約20種類を最初に一画面で見せる。
見つからない食材だけ、カテゴリーまたは検索から追加する。

登録速度が速い可能性があるため、比較案として残す。

### 共通する操作方針

- 数量入力は求めず、選択時は「ある」で登録する
- 食材イラストをタップすると、冷蔵庫の中へ入る
- 保存場所は食材ごとの一般的な場所へ自動配置する
- 必要な場合だけ後から保存場所を直す

**実装状況：部分実装**

- 実装済み：イラストをたどって食材を選ぶ流れ、保存場所の初期値の自動判定
- 方針とのズレ：**追加時に残量・単位の入力を求めている**（数量欄が必須）。「選択時は『ある』で登録」になっていない

### ほかの登録方法

- レシート読み取り：初回登録後、買い物を在庫へ反映する補助機能
- 文字入力：一般食材にないものを追加する補助機能
- 冷蔵庫写真の認識：難易度が高いため、当面の中心機能にしない

**実装状況：実装済み**（レシート読み取りは端末内OCR。文字入力は一覧にない食材の補助として用意。写真認識は未着手のまま）

## 初回オンボーディングの設計（2026-07-25 決定）

方針書初版のカテゴリー式（野菜→肉魚→…を7画面で回す）は、**第一案から「あとで足す精度向上機能」へ格下げする**。
代わりに、**最初の一食を決める過程をそのまま登録にする**入口を作る。指標はオンボーディング完了率ではなく、
**最初の正直で有用な提案までの時間**。

1. 「今夜使えそうな主役」を **1〜2品**選ぶ（肉・魚・卵・豆腐・麺・ごはんなど）
2. 選択から暫定の料理候補を3件出す
3. 候補を分けるのに必要な食材だけ聞く（「ごはんはありますか？」）
4. 答えた食材が冷蔵庫イラストへ入る
5. 一食決まったら通常画面へ。あとから「冷蔵庫をもっと正確にする」で続けられる

**実装状況：実装済み（2026-07-26）**

初回だけ出る2画面。1画面目は主役20品（`LEAD_INGREDIENTS`）を肉・魚介・卵豆腐・
主食麺の4組に分けて並べ、1〜2品タップする。2品でいっぱいのときは古いほうを
押し出す（「2つまでです」と断るより選び直せるほうが速い）。

2画面目は候補3件を上に出し、その下に「候補を分けるのに効く食材」を6品だけ出す。
タップするたび候補の並びと「材料あり／あと◯◯」が変わる。全食材を並べると
格下げしたカテゴリー式へ戻ってしまうので、上位8候補で使われている数の多い順に
6品へ絞っている。

登録は「ある・量は不明」で入る。数量は聞かず、最初に料理を作るときに確認する。

「あとで入れる」は空の冷蔵庫で始める（サンプルを入れ直すより正直）。
5の「冷蔵庫をもっと正確にする」入口も実装した（下記）。

### 主役は1〜2品（2品は必須にしない）

実データで確認したところ、**1品の方が候補は豊富**で、2品を「両方必須」で絞ると候補が激減する。

- 1品あたりの候補数：卵16件、ごはん11件、ひき肉8件、鶏むね7件、豚こま・豆腐6件、麺類・青魚は1〜2件
- 2品を両方必須にすると：卵+ごはん6件、豚こま+キャベツ4件、鶏むね+卵1件、**豚こま+卵は0件**

2品目を AND（両方必須）で実装すると行き止まりが生まれる。**AND ではなく加点**として扱い、
両方使うレシピを最優先に、片方だけのレシピも候補に残す。既存の `priorityBoost` に乗せられる。

なお AND と加点のどちらにするかは、**実際の使用感を見てから最終決定する**（2026-07-25 時点は保留）。

**実装（2026-07-26）：加点で入れた。**さらに実装中に分かった問題を1つ直している。
豚こま＋卵のように**両方使うレシピが0件**の組み合わせでは、材料の少ない側
（卵）だけが上位を占め、**選んだのに一度も出てこない主役**が生まれた。
聞いた意味が無くなるので、2品選んだときはそれぞれの最上位を先に持ってくる。

候補が3件に届かない主役（麺類・青魚）のために、主役以外からも候補を補うフォールバックが必要。

### 数量は「捨てる」のでも「盛る」のでもなく、確信度を持たせる

カタログ初期値のままでは1人分すら足りないレシピが77件中30件ある（卵1個に対し要求2個など）。
これに対して検討した2案は、どちらも退けた。

- **数量比較をやめて3状態だけにする案**：人数変更（1人分→4人分）で可否を再判定できず、「少ない」を理由に本当は作れる料理まで候補から落ちる
- **初期値を買った実量へ引き上げる案**：初回の「ある」は新品購入ではなく半端に残っている物である可能性が高く、在庫の過大評価と「偽の精密さ」を生む

採るのは、数量を保持したまま**確信度**を持たせる形。

```
stockState        : ある ／ 少ない ／ ない        （ユーザーへ見せる一次状態）
quantity          : 数値 ／ null
quantityConfidence: 確認済み ／ 推定 ／ 不明
reviewed          : カテゴリー単位（未確認を「ない」と扱わないため）
```

オンボーディングのタップ時は「ある・quantity=null・不明」で登録する。
「買った」と記録した食材だけ、標準購入量から推定値を入れる。

### 確信度は画面に出さない

**確信度はデータには持つが、提案リストには出さない。** リストに3段階を並べると比較が難しくなり、
「記憶と判断の負担を減らす」という核に反する。アプリの自信のなさはユーザーの関心ではない。

文言は次で確定（2026-07-25）。

| 場所 | 文言 |
|---|---|
| リスト（材料がそろっている） | **材料あり** |
| リスト（不足） | **あと 卵2個** |
| ボタン（数量が確認済みで足りる） | これを作る |
| ボタン（数量が未確認） | **量を見て作る** — 状態ではなく行為で示し、質問が来ることを予告する |

「作れそう」「たぶん作れます」「あとは量だけ」はいずれも却下した。曖昧だからではなく、
**判断をユーザーへ投げ返している**ため。核に反する。

数量の確認は、リストではなく「これを作る」を押した後に、必要な食材だけ1問聞く（「卵は2個ありますか？」）。

**実装状況：実装済み（2026-07-26）**

量が未確認の食材を持つレシピは、ボタンが「量を見て作る」になる。押すと確認画面の
「最低限必要」の中で、その食材だけが押せる状態表示（「ある」／「足りない」）になる。
**既定は「ある」**で、実物を見て違うものだけ外してもらう（毎回タップさせない）。

「ある」で作ると、その食材の量を必要量まで上げて確認済みにする。「足りない」の
ままにしたものは不明で残し、次に作るときまた聞く。

人数を変えると答えは破棄して聞き直す。「2人分の卵」への答えは1人分では意味を
持たないため（2人分では足りなくても1人分なら足りることがある）。

### 空振りを行き止まりにしない

「材料あり」を信じて選んだのに、確認画面で足りないと分かる空振りは必ず起きる。
そのとき「戻る」だけを出さず、**「1人分にする」「このまま作る」「別の候補へ」**を並べる。
量が足りないことは失敗ではなく、人数を落とすか妥協するかの選択でしかない。

**実装状況：実装済み（2026-07-26）**

「足りない」と答えたときだけ、確認画面に「1人分にする」（2人分以上のとき）と
「このまま作る」が出る。キャンセルボタンの文言は「ほかの候補へ」にした。
「このまま作る」は在庫を引けるところまで引く（0で止まるので負にはならない）。

数値で不足しているもの（持っていない・明らかに足りない）にはこの逃げ道を出さない。
それは買い足しの話で、妥協の話ではないため。

### 実装の順序

1. サンプル撤去＋主役食材の簡易登録＋「数量未確認」の導入を**同時に**出す（サンプル撤去だけ先に出すと一時的に最も弱い状態になる）
2. 「材料あり」と不足表示の分離
3. 調理直前の必要量確認
4. 調理後・翌日の3状態補正
5. カテゴリー式「冷蔵庫をもっと正確にする」

**進捗（2026-07-27）：1〜5をすべて実装した。**

5「冷蔵庫をもっと正確にする」は、冷蔵庫画面の上の入口から入る。10の売り場を
順番に出し、家にあるものをタップするだけ（量は聞かない。登録は初回登録と同じ
「ある・量は不明」）。すでに入っている食材は選んだ形で出し、押せなくしてある。
**この画面で入れたものだけ、もう一度押して取り消せる**（前から入っていた在庫を
ここから消せてしまうと事故になるため）。

見終わった売り場は覚えておき、入口に「10の売り場のうち◯を見ました」と出す。
**この記録は在庫の判定には使わない。** 見ていない売り場を「その食材は無い」と
決めつけないためで、用途は進み具合の表示だけ。方針書の
`reviewed: カテゴリー単位（未確認を「ない」と扱わないため）` はこの形で入れた。

初回に押し付けず、いつでも「やめる」で抜けられる。カテゴリー式を第一案から
格下げした判断はそのままで、ここはその格下げ後の姿。

### 実装前に解いておく課題

- **既存ユーザーのサンプル在庫を無条件削除できない。**（解決済み・2026-07-26）
  在庫アイテムに `origin` を足し、これから入るサンプルには印を付けた。印の無い
  保存済みデータについては、**数量・単位・場所が初期値のままで、いま在庫にある
  ものだけ**を候補にする。冷蔵庫画面の上に一度だけ案内を出し、何を消すのかを
  並べて「片付ける／このまま使う」を選ばせる。押すまで消さない。
  量を変えたキャベツが消えないことは実機で確認した（180g→100gにしたキャベツは
  対象外になり、初期値のままの卵だけが片付いた）。
- 履歴の取り消しとの整合。3状態を持つなら、変更前のスナップショットを履歴へ含める
- 主役候補にできない食材への対応。食パンは必須に含むレシピが0件だったため、2026-07-25にパン系5件を追加して解消した（→[RECIPE_RESEARCH.md](RECIPE_RESEARCH.md)）

## 現時点で残している比較案

- 初回登録：カテゴリー方式を第一案、定番20品方式を比較案として保持
- 初回登録の操作：冷蔵庫を表示したまま、カテゴリー食材を棚へ入れる体験を採用予定
- 栄養表示：初期画面は簡潔にし、必要な人だけ設定で表示

決定済みのため比較案から外した項目：

- レシピの収録量と記述の深さ → 「件数は多く、記述は簡易に」で確定（2026-07-25）
- レシピ詳細の外部連携 → Google・YouTubeの検索リンクとして実装済み

## 実装が方針へ追いついていない項目

方針を決めたのに未実装、または方針と逆になっている項目。上から順に効きが大きい。

1. **初回の冷蔵庫登録**（未実装）— サンプル5品が入っているため、初回の提案が実際の冷蔵庫と無関係になる
1. **料理の完成イラスト**（生成中）— Codexへの依頼は `assets/CODEX_PROMPT_RECIPE_ART.md`（77品・7シート）
2. **昼食入力からの推薦調整**（未実装）

解消済み：残量の3状態表示（2026-07-31、帯を3段階に離散化）
解消済み：栄養表示のオン・オフ、初回の冷蔵庫登録、翌日の在庫確認・補正、
登録時に数量を求めない（初回登録は「ある・量は不明」で入る）、カテゴリー式で
冷蔵庫をもっと正確にする。2026-07-26〜27

## 次に討論する項目

- カテゴリー方式の画面遷移と、登録完了までの長さ
- 昼食入力機能を初期状態でオンにするか
- 「少ない」をどのような食材イラストで表現するか
- 今日のおすすめを決める最優先基準
- 帰り道で冷蔵庫を開くまでの操作数

## 方針の外にある実装上の課題

方針書の話ではないが、上記を実装する前に決着させる必要があるもの。

- ごま油は単位「小さじ」が在庫単位に無いため、いまも選択できない。調味料20品目を在庫として持つのか、文字だけの常備品として別枠にするのかが未決
- 賞味期限を持っていないため、使い切りの緊急度が手動フラグ頼み
- データがlocalStorageのみで、書き出しも端末間同期も無い

**解消済み**：単位の完全一致依存（`UNIT_CONVERSIONS` で換算）、代替食材（`INGREDIENT_SUBSTITUTES`）、かつお節がイラスト一覧に無い問題、PWA未対応（2026-07-26。ホーム画面追加とオフライン起動に対応。「帰り道に確認する」が最重要の入口だったため）
