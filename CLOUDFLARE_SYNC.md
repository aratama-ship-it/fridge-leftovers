# 二人で1つの冷蔵庫を共有する（Cloudflare版）

作成日: 2026-07-30 / 対象: 冷蔵庫アプリの共有機能

先に [Supabase版](SUPABASE_SETUP.md) を作ったが、**無料プランは7日使わないと
プロジェクトが止まる**（公式に明記。「We may pause applications on the Free Plan
that exhibit low activity in a 7-day period」）。旅行から帰って開いたら動かない、が
起こる。Cloudflareのドキュメントにはこれに相当する記述が見当たらないため、
そちらへ移す。

**同期の考え方（1品1行・版を申告・在庫は少ないほうを採る）は Supabase版のまま。**
変わるのは接続先と、認証の代わりの仕組み。

---

## 0. なぜ設計が簡単になるか

Cloudflareには認証の機能が無い。Supabase版ではアカウント・確認メール・
行レベルセキュリティ・招待コードを使ったが、**二人で使うだけならアカウント自体が要らない。**

**冷蔵庫ID方式**：推測できないランダムな文字列を1つ発行し、それを知っている端末だけが
読み書きできる。共有はリンクかQRを1回渡すだけ。

- 「知っている人だけが見られる限定公開リンク」と同じ考え方
- メールアドレスもパスワードも要らない。確認メールの往復も無い
- アカウント上の「誰か」は分からない。代わりに、本人が付けた端末名を最終変更の目印として記録する

### 承知しておくこと

- **リンクが漏れたら中身を見られる。** 冷蔵庫の中身なので実害は小さいが、そういう性質
- IDは推測できない長さにする（下記は22文字。総当たりは現実的でない）
- 気になるなら、あとから4桁の暗証番号を足せる（IDと合わせて確認する形）
- 端末名は「台所のiPad」のような自己申告の表示名で、本人確認やアクセス制御には使わない

## 1. 何を共有し、何を共有しないか（Supabase版と同じ）

| データ | 共有 | 理由 |
|---|---|---|
| 在庫（冷蔵庫の中身） | **する** | これが共有したいもの本体 |
| 買い物リスト | **する** | どちらが買っても消える必要がある |
| 履歴（調理・使い切り・廃棄・在庫修正） | **する** | 相手側の操作と在庫への反映も分かる |
| 棚の数 | **する** | 冷蔵庫そのものの形 |
| 設定・最近追加した食材・「今日使いたい食材」 | しない | その人の見え方といまの文脈 |

## 2. 競合したらどうするか（Supabase版と同じ）

- **在庫の数量が競合したら、少ないほうを採る。** 多く見積もると「材料あり」と出て
  買い物に行かず、帰ってから足りないと分かる。少なく見積もれば余分に買うだけで済む
- **「使い切った」は必ず勝つ**
- **確信度は低いほうを採る**（確認済み < 推定 < 不明）
- **買い物リストは足し合わせる。** 消したものは消えたまま
- **履歴は通常追記**し、調理で使った食材を直したときだけ該当行を更新する

決着は**端末側で付ける**。サーバーは「あなたが見ていた版と違う」と返すだけにして、
何を採るかはアプリの都合なのでアプリが決める。

競合応答は端末がその場でサーバー状態と混ぜる。`seq` はPOST応答では進めず、GETで
受け取った変更だけを既読にする。サーバー側の書き込みは版を条件にしたCASで片方だけを通す。

## 3. 置き場所

- **Cloudflare Workers**（無料枠 10万リクエスト／日）… 受け口
- **Cloudflare D1**（無料枠 5GB・読み500万行／日・書き10万行／日）… 置き場

このアプリの想定（二人・在庫30品・1日に何度か開く）は1日100リクエスト前後、
読み3,000行前後、書き50行前後。**1000倍以上の余裕がある。**

## 実装状況（2026-07-30）

**動いている。** 公開先は `https://fridge-leftovers.juggler-arata.workers.dev`。

実機（ブラウザ2つを別端末に見立てて）で通したこと:

- 「共有をはじめる」→ 冷蔵庫ができ、いまの中身が全部サーバーへ行く
- 相手がリンクを開き、参加前の確認で内容の扱いを選ぶと中身が届く（登録画面は出ない）
- 逆向きも届く（相手が豆腐を使い切り、キャベツを足したのが反映される）
- **競合が「少ないほう」で決着する。** 卵を4個にした端末と1個にした端末が
  ぶつかって1個になった。2回目の同期でも揺れない
- 共有中は「この端末のブラウザ内だけに保存されます」という表示が
  「この端末と、共有している相手の端末で見られます」に変わる

### 参加前の確認（2026-08-02）

招待リンクを開いただけでは同期しない。参加前に、この端末の在庫・買い物・履歴の
件数を見せて、次のどちらかを本人が選んでから通信する。

- **共有の内容に入れ替える**：この端末の既存内容は共有先へ送らず、相手の内容を受け取る
- **今の内容も合わせる**：この端末の既存内容も共有先へ送り、相手の内容と統合する

空の端末では、意味が同じになる統合の選択肢は出さない。受信・送信の途中で通信に
失敗した場合は、在庫・買い物・履歴・棚・同期状態を参加前のスナップショットへ戻す。
ブラウザのスマートフォン幅で、リンクからの確認、キャンセル、手入力からの確認、
存在しないIDで両モードが失敗した場合の復元を確認した。

### 操作後の自動同期（2026-08-07）

共有中はトップ上部に同期ボタンを表示する。在庫・買い物・履歴・棚の数を保存した
あとは、最後の操作から600ミリ秒待って自動で1往復する。調理のように在庫と履歴を続けて
保存する操作も通信は1回へまとめる。受信した内容のローカル保存からは再送しない。

通信に失敗した変更は「未同期」として残し、オンラインへ戻ったとき、画面へ戻ったとき、
またはトップ上部の同期ボタンから再試行できる。サーバーから端末へ常時配信する方式では
ないため、操作していない相手側の画面は、画面復帰か同期ボタンで最新内容を受け取る。

### 最終変更の時刻と端末（2026-08-05）

在庫・買い物・履歴の各データに、最終変更時刻と、その時点の端末名を表示する。
端末名は設定で変更できるが、過去のデータには変更時点の名前を残す。これは完全な
操作履歴ではなく「最後にどの端末で触ったか」の軽量な記録である。

- 変更時刻は端末時計、`receivedAt` はサーバーが受け取った時刻
- 旧版から届いた変更は受信時刻で補い、端末名を「端末不明」と表示する
- 既存D1へは `worker/migrations/0002_change_attribution.sql` をWorker公開前に1回適用する

## 4. 手順（本人の作業）

`worker/` に一式が入っている。ターミナルで次を順に実行する。

```bash
cd "/Users/arata/Library/Mobile Documents/com~apple~CloudDocs/claude code files/apps/life-app/fridge-leftovers/worker"

# 1. Cloudflareにログイン（ブラウザが開く）
npx wrangler login

# 2. データベースを作る。出力の database_id を控える
npx wrangler d1 create fridge-leftovers

# 3. 控えた database_id を wrangler.toml の該当箇所へ貼る
#    （database_id = "ここ" の行）

# 4. テーブルを作る
npx wrangler d1 execute fridge-leftovers --remote --file=./schema.sql

# 5. 公開する。出力の https://....workers.dev を控える
npx wrangler deploy
```

最後に出た URL を教えてもらえれば、アプリへ組み込む。

> `npx wrangler` は初回だけ「インストールしますか」と聞いてくる。y で進む。
> このMacに wrangler は入っていないが、npx が都度取ってくるので入れなくてよい。

## 5. サーバーがやること（全部で3つ）

| | 何をするか |
|---|---|
| `POST /v1/fridges` | 新しい冷蔵庫を作り、IDを返す |
| `GET /v1/fridges/:id/changes?since=N` | Nより後の変更を返す |
| `POST /v1/fridges/:id/changes` | 変更を適用する。版が食い違えばサーバー側の行を返す |

**サーバーは冷蔵庫の中身を解釈しない。** 食材の名前も数量も、ただのJSONとして
預かるだけ。レシピや判定はすべて端末側にある。

## 6. 版の付け方

各行に2つの数字を持つ。

- `version` … その行が何回書き換わったか。端末は「自分が見ていた版」を申告し、
  一致したときだけ書き換わる。違えば競合として返る
- `change_seq` … 冷蔵庫ごとの通し番号。端末は「前回どこまで受け取ったか」を覚えて
  おき、続きだけをもらう

`change_seq` は冷蔵庫ごとに1つの数を増やして振る。追加でも更新でも必ず進めるので、
更新を取りこぼさない。
