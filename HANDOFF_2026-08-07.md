# 引き継ぎ資料 — 2026-08-07時点

冷蔵庫在庫管理＋料理提案アプリ。GitHub Pages公開中
（https://aratama-ship-it.github.io/fridge-leftovers/ ）。

**このファイルは自己完結を意図している。** 別マシンのエージェント（Codex含む）が
このファイル単体を読んで作業を引き継げるよう、パス・手順・前提をここに明記する。
旧版は `HANDOFF_2026-08-03.md`（事実が古くなったため本ファイルへ置換。経緯参照用に残置）。

## 0. 場所

このMacでは:

```
/Users/arata/Library/Mobile Documents/com~apple~CloudDocs/claude code files/apps/life-app/fridge-leftovers
```

（2026-08-02の再編前の旧パスは `app-dev/fridge-leftovers`。ドキュメント内の旧パス表記は
2026-08-07に一括修正済み。）

`.git` はiCloudでの破損を避けるため `--separate-git-dir` で外出ししてある。
このMacでは `~/git-repos/fridge-leftovers`。

## 1. 公開済みの状態（origin/main = cda5df5 相当、2026-08-07）

旧HANDOFFの懸案は以下のとおり解決済み:

- **共有参加前の確認ダイアログ**は PR #2（`agent/share-join-confirmation`）でmainへマージ済み
- その後 `codex/top-auto-sync` ブランチで14コミットが積まれ、PR #3〜#5で全てマージ済み。
  内容: レシピを **240件へ拡張**（基礎120件＋`recipe-expansion.js` の120件、手順文は機械生成）、
  季節レシピ、履歴の耐久化（上限1000件）、iPad miniレイアウト、賞味期限アラート（任意）、
  買い物チェックリストのコンパクト化、操作ごとの共有同期、栄養ヘルプ等
- APP_VERSION は **0.18.0**。CI 8本すべて緑、Pages デプロイ済み（2026-08-07 13:28 UTC確認）

食材イラストは323点、料理イラストは77点。追加120レシピは既存食材イラストの
代替絵割当で表示する設計（専用の料理イラストは無い）。

## 2. 既知の不具合候補（2026-08-07のClaude×Codexレビュー会議で特定）

**1〜5は修正してmainへマージ・公開済み（2026-08-08 00:45頃）。6のみ未着手。**
ただし**いずれも実機確認は未実施**。コード上のテストは通っているが、実際の
二端末操作・PWA更新・バックアップ読み込みでの確認は次の作業。

1. **【最重大・修正済/公開済】同期の競合処理が相手の変更を上書きする** —
   `app.js` の共有同期処理。競合応答の `server.body` をマージせず版番号だけ更新し、
   直後に `state.share.seq` をPOST応答のseqまで進めていた。次回同期の取得
   （`since=seq`）で相手の変更が飛ばされ、自分の内容が新版として通り
   **相手の変更が黙って消える**状態だった。
   → PR #7: 受信1件分を `applyOneIncoming()` に切り出して競合応答でも `mergeEntity`
   を通す／POST応答でseqを進めるのをやめる（seqはGETでのみ前進）。
2. **【修正済/公開済】Worker側の版チェックが非原子的** —
   `worker/src/index.js` の `applyOne()`。SELECT→seq更新→UPSERTが別ステートメントで、
   同じ `baseVersion` の並行リクエストが両方通り得た。
   → PR #7: 版を条件にしたUPDATE／`on conflict do nothing` のINSERT（CAS）にし、
   書き込み行数0なら再SELECTしてconflictを返す。応答JSONの形は不変。
   並行書き込みテストを `worker/check-worker.mjs` に追加（旧コードでは落ちることを確認済み）。
3. **【修正済/公開済】PWA更新時、取得失敗を握り潰して旧キャッシュを削除** — `sw.js`。
   → PR #8: 1件でも取れなければinstallを失敗させ `skipWaiting` を呼ばない。
   activateでも全件揃っているか確かめてから旧キャッシュを消す。
4. **【修正済/公開済】追加レシピ読込失敗で白画面** — `app.js` 冒頭。
   → PR #8: 空パックへ縮退して基礎レシピだけで起動し、トーストで1回知らせる。
   120件ちょうどの検査は既存CI（`check-recipe-logic.mjs`「追加データパックは120件」）が持つ。
5. **【修正済/公開済】バックアップ読込が検証前にlocalStorageを上書き** —
   → PR #8: `readBackup()` にセクションごとの型検査を足して書き込み前に弾き、
   `applyBackup()` に `shareJoinSnapshot()` と同じやり方のロールバックを足した。
6. **`loadInventory()` の壊れデータ時のフォールバックがサンプル5品** — 保存値が
   配列でないと `cloneDefaults()`（サンプルの5品）へ落ちる。5と重なると
   **本人の冷蔵庫がサンプルデータで置き換わりうる**。空配列＋初回案内のほうが
   実害が小さいと思われるが、既存の初回案内の出し方と絡むため要検討（未着手）。

修正の優先順位（会議合意）: ①同期の正しさ（1＋2＋二端末並行テストのCI追加）→PR #7で対応
②追加120レシピの加熱安全性の人手監査（生肉・魚・卵、レンジ料理の手順文）
→ 2026-08-08 02:00の一回限り夜間タスク `fridge-recipe-safety-audit` が
`RECIPE_SAFETY_AUDIT_2026-08-08.md` へ判定レポートを出す（**提案のみ・修正はしない**）
③耐障害性（3＋4＋5）→PR #8で対応。6は未着手。

## 2-1. マージ済みPR（2026-08-08 00:45頃、いずれもCI緑・Pages公開済み）

- **PR #6** `docs/handoff-refresh`: 旧パス表記の一括修正、`private-test-fixtures/` の
  公開除外、READMEの追記。アプリ本体の変更なし
- **PR #7** `fix/sync-conflict-merge`: 不具合1・2の修正とテスト追加
- **PR #8** `fix/resilience`: 不具合3・4・5の修正とテスト追加
- **PR #9** `fix/asset-cache-split`: 更新のたびにイラスト28MBを捨てて取り直していた
  問題の修正。キャッシュを版つき（`SHELL_CACHE`）と固定名（`ASSET_CACHE`）の2層に分け、
  activateで消すのは版つきのみにした。古い版のアセットは削除前に引き継ぐ。
  **`sw.js` の自動テスト `scripts/check-sw.mjs`（9項目）を新設しCIへ追加**（CIは9本に）

公開後の `sw.js` は VERSION `867650ad` / `app.js?v=6a93b181`。配信中の内容が
手元のビルドと一致することをcurlで確認済み。

**mainへのマージ = GitHub Pagesへの公開。** 一般リリース前のため、実機で試すには
公開してしまうほうが手軽、という判断（2026-08-08 本人）。

## 2-2. 実機で確認すること（未実施・次の作業）

コード上のテストは通っているが、以下は実際の端末でしか確かめられない。

1. **同期の競合（不具合1）**: 二端末で同じ食材を同時に触る。片方で数量を減らし、
   もう片方で別の数量にしてから両方同期 → **少ないほうが残り、どちらの変更も
   消えていない**こと。修正前は後から同期したほうが相手の変更を消していた
2. **PWA更新（不具合3）**: ホーム画面から開いた状態で新しい版を配信し、
   機内モードにしてから開き直す → 起動できること（不完全なキャッシュに
   乗り換えていないこと）
3. **バックアップ読み込み（不具合5）**: わざと壊したJSON（`inventory` を配列以外に
   書き換えたもの）を読み込ませ、**エラーが出て今の冷蔵庫が変わらない**こと。
   修正前はサンプル5品に置き換わりうる状態だった

なお、このHANDOFFファイル自体はgitに追跡させていない（旧版も同様の扱いだった）。
このMacの作業ツリーにのみ存在する。

## 3. 未決事項（本人判断待ち）

- `assets/workspace-import-2026-08-02/`（37MB・未追跡）の採用/破棄。中身は
  `atlas-src/23/` と `23-keyed/`（各12枚、現行23番シートとSHA-256が異なる/本体に無い）。
  フォルダ内README.mdに検証記録あり。通常のアトラス生成入力には混ぜていない
- 調味料20品目の扱い（ごま油が単位「小さじ」のため在庫単位に無く選択できない）。
  単独修正でなく「調味料を在庫管理するか、pantry扱いにするか」の設計判断が先
- 昼食入力からの推薦調整（方針書で唯一の未実装。実利用で要否を見極めてから）
- 使用感フィードバック（本人へ確認中・未回答）: 主役食材の絞り込みの強さ／
  翌日カードの出過ぎ／レシート精度／冷蔵庫→パントリーのドラッグの挙動
  （※「同期のタイミング」は上記不具合1の修正へ格上げ）
- 旧 `HANDOFF_2026-08-03.md` の削除可否

## 4. アーキテクチャ早見表

- **ビルド不要の素のJS**。`app.js`（1本、9000行規模）・`recipe-expansion.js`
  （追加レシピ120件、app.jsより先に読み込む）・`index.html`・`styles.css`。
  GitHub Pagesの`main`ブランチがそのまま公開元
- **食材・料理イラスト**: 1254×940のPNGアトラス（4列×3行=12個/枚）。生成はCodex CLI委譲。
  マゼンタ縁は`scripts/despill-magenta.mjs`で機械検出・修正
- **同期**: `worker/src/index.js`（Cloudflare Workers + D1、
  サーバーは `https://fridge-leftovers.juggler-arata.workers.dev` ）。認証の代わりに
  22文字の推測不能な冷蔵庫ID。競合解決は「在庫は少ないほう・使い切ったが必ず勝つ・
  確信度は低いほう・棚は多いほう」（`mergeEntity()`）
- **PWA**: `sw.js`のVERSIONと先読みリストは`scripts/cache-version.mjs`が自動生成。
  **app.js/styles.css/recipe-expansion.jsを変えたら必ず
  `node scripts/cache-version.mjs` を実行してからコミット**
  （忘れるとCIの「キャッシュ回避クエリの整合」で落ちる）
- CI（`.github/workflows/checks.yml`）は8本。**Node 22系必須**（`node:sqlite`使用）

## 5. このMac固有の環境メモ

- レシートOCRの私的テスト画像は `private-test-fixtures/`（未追跡・.gitignore済み。
  購入情報を含むため公開リポジトリへ入れない）
- Codex CLIは `codex exec --skip-git-repo-check --sandbox workspace-write -C "<dir>" "<指示>" </dev/null`
- **指示文はファイルに書いて `"$(cat /path/prompt.md)"` で渡す。** 指示文を直接
  二重引用符で書くと、中のバッククォートや `${...}` をシェルが展開してしまい、
  Codexが起動せず `command not found: ...` で落ちる（2026-08-08に実際に発生）
- **`codex exec resume` は `--sandbox` と `-C` を受け付けない**（`unexpected argument`）。
  継続したい内容も、現状を前提として書き直して通常の `codex exec` で投げるほうが確実
- ローカルプレビューは `python3 -m http.server 8467` を直接起動するのが確実
  （旧HANDOFFに記載のgetcwd権限エラーは2026-08-07時点で再現せず。再発したら記録する）
