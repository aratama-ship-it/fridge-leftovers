# 引き継ぎ資料 — 2026-08-03時点

> **【旧版】この資料は事実が古くなった。最新は `HANDOFF_2026-08-07.md` を読むこと。**
> （2-1の共有参加ダイアログはPR #2でマージ済み。レシピは240件に拡張済み。）

冷蔵庫在庫管理＋料理提案アプリ。GitHub Pages公開中
（https://aratama-ship-it.github.io/fridge-leftovers/ ）。

**このファイルは自己完結を意図している。** 別マシンのエージェント（Codex含む）が
このファイル単体を読んで作業を引き継げるよう、パス・手順・前提をここに明記する。
より詳しい話は各節末尾で挙げるドキュメントを直接読むこと。

## 0. 場所（★2026-08-02にパスが変わった）

このMacでは:

```
/Users/arata/Library/Mobile Documents/com~apple~CloudDocs/claude code files/apps/life-app/fridge-leftovers
```

**旧パスは `app-dev/fridge-leftovers`。** 2026-08-02のワークスペース大再編で
`apps/life-app/` 配下へ移った。ドキュメント内やスクリプトのコメントに旧パスが
まだ残っている箇所があるかもしれない。見つけたら実害は無いが、コマンド例として
コピペする前にパスを読み替えること。

`.git` はiCloudでの破損を避けるため `--separate-git-dir` で外出ししてある。
このMacでは `~/git-repos/fridge-leftovers`。`.git` ファイル自体は数十バイトの
ポインタなので、これもiCloud側にある。

## 1. 公開済みの状態（origin/main = HEAD、コミット d777428）

2026-08-01時点で、以下はすべてコミット・push・CI緑・実機確認済み。

- **料理77件・食材323品目、完成イラストは食材323点＋料理77点の全件が揃っている**
  （最後のシートr06・r07は2026-08-01に統合）
- **PWA化**（ホーム画面追加・オフライン起動）、**栄養表示ON/OFF**、**残量3段階表示**
- **二人での共有はCloudflare Workers + D1で稼働中**（Supabase版は無料枠の7日休止制約で
  見送り、`SUPABASE_SETUP.md`に保留として残置）。サーバーは
  `https://fridge-leftovers.juggler-arata.workers.dev`
- 起動が重いときに操作をブロックする読み込み画面、フッターの最下段固定（2026-08-01）
- 方針書`PRODUCT_DIRECTION.md`の未実装は**昼食入力からの推薦調整**のみ

CI（`.github/workflows/checks.yml`）は8本：初期単位／主役リストの整合／レシート照合／
共有サーバー／レシピ判定／キャッシュ整合／アイコン整合／アトラス背景抜け。
CI用Node.jsは**22系必須**（`node:sqlite`を使うため）。

このあたりの経緯や設計判断は`README.md`・`PRODUCT_DIRECTION.md`・`CLOUDFLARE_SYNC.md`・
`RELEASE_CHECKLIST.md`に分けて書いてある。

## 2. ★未コミットの作業が残っている（重要）

**このMacの作業ツリーに、コミットされていない変更が乗っている。** 誰の作業かは
このセッションでは特定できない（プロセスは現在動いていないので、少なくとも
今まさに書きかけではない）が、内容から判断して意図の明確な、完成度の高い実装。
**中身を読まずに上書き・破棄しないこと。**

### 2-1. 共有参加前の確認ダイアログ（app.js・styles.css・index.html）

「招待リンクを開いただけでは同期しない」という設計変更。参加前に

- この端末の在庫・買い物・履歴の件数を見せる
- 「共有の内容に入れ替える」（この端末の内容は送らない）／
  「今の内容も合わせる」（送って統合する。端末が空のときはこの選択肢自体を出さない）
  のどちらかを選ばせる
- 通信の途中で失敗したら、参加前の状態（在庫・買い物・履歴・棚・同期状態）へ
  スナップショットから復元する

`CLOUDFLARE_SYNC.md`と`RELEASE_CHECKLIST.md`にも対応する節が追加済みで、
「ブラウザのスマホ幅で、リンクからの確認・キャンセル・手入力からの確認・
存在しないIDで両モードが失敗した場合の復元を確認した」という実機確認の記録がある。

**このセッションで確認したこと**：構文OK、既存の自動チェック8本すべて通過、
`cache-version.mjs --check`も一致（`?v=`の更新込みで既にコミット可能な状態）。
つまり**技術的にはコミットして問題ない状態**だが、本人が最終確認済みかどうかは
このセッションからは分からないので、コミットは保留にしてある。

次にこれを触るエージェントへ：`git diff app.js styles.css index.html` で中身を見て、
本人に「この共有参加の確認ダイアログをコミットしていいか」を確認してから進めること。

### 2-2. 未追跡フォルダ `assets/workspace-import-2026-08-02/`（37MB）

2026-08-02の大再編のとき、ワークスペース直下の旧`assets/`に残っていた食材画像候補を
「同名ファイルを上書きしない」方針で保全した退避フォルダ。中身は`atlas-src/23/`と
`atlas-src/23-keyed/`（各12枚、本体の現行23番シートとSHA-256が異なる/本体に無い）。
フォルダ内の`README.md`に詳細と移動時のSHA-256検証結果がある。

**採用するか破棄するかは未決。** `scripts/build-atlas.mjs`の通常入力には混ぜていない。
本体側`README.md`にもこのフォルダへのリンクが追加済み。

## 3. アーキテクチャ早見表

- **ビルド不要の素のJS**。`app.js`（1本、8000行超）・`index.html`・`styles.css`。
  GitHub Pagesの`main`ブランチがそのまま公開元
- **食材・料理イラスト**：1254×940のPNGアトラス（4列×3行=12個/枚、
  `background-size: 440% 330%`）。生成はCodex CLI（`codex exec`）に委ねている。
  抜き作業特有の「マゼンタの縁」問題は`scripts/despill-magenta.mjs`で機械的に検出・修正
- **同期**：`worker/src/index.js`（Cloudflare Workers）。認証の代わりに22文字の
  推測不能な冷蔵庫IDを使う。競合解決は「在庫は少ないほう・使い切ったが必ず勝つ・
  確信度は低いほう・棚は多いほう」
- **PWA**：`sw.js`のVERSIONと先読みリストは`scripts/cache-version.mjs`が
  ファイル内容から自動生成する。**app.js/styles.cssを変えたら必ずこのスクリプトを
  実行してからコミットする**（忘れるとCIの「キャッシュ回避クエリの整合」で落ちる）

## 4. このMac固有の環境メモ

- ローカルプレビューは`.claude/launch.json`の`fridge-leftovers`エントリ
  （`python3 -m http.server 8467`）。**2026-08-03時点で、この設定を使った
  プレビュー起動ツールが `getcwd: cannot access parent directories: Operation not permitted`
  で失敗する現象を確認した**（Xcode同梱のPython3.9経由で起動されるが、
  シェルのcwd取得で権限エラーになる）。手元では`cd`して直接
  `nohup python3 -m http.server 8467 &`する回避策で動く。根本原因は未調査
- Codex CLIでの画像生成は`codex exec --skip-git-repo-check "..." </dev/null`
  （新規）・`codex exec resume --last --skip-git-repo-check "..." </dev/null`（継続）。
  1回の実行で1〜3シート（12品/シート）進む。1シートあたり約39万トークン

## 5. 次に検討すること

- 上記2-1（共有参加ダイアログ）のコミット可否を本人に確認する
- 上記2-2（workspace-import）の採用/破棄判断
- 昼食入力からの推薦調整（方針書で唯一残っている未実装。実際の利用を通して
  本当に要るか見極めてから着手する方針で保留中）
- 調味料20品目の扱い（ごま油が単位「小さじ」のため在庫単位に無く選択できない）
- 使用感フィードバック（本人へ確認予定・未回答）：主役食材の絞り込みの強さ／
  翌日カードの出過ぎ／同期のタイミング／レシート精度／冷蔵庫→パントリーの
  ドラッグの挙動
