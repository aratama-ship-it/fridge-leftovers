# iCloudの同期競合コピー(「app 2.js」等)は追跡しない
* [0-9].*

# イラストの元素材（1枚2MB前後・全部で500MB超になる）。
# アトラスに焼き込んだ assets/ingredient-atlas-*.png が成果物で、
# 素材は再生成・切り抜き直しのために iCloud 上へ置いておくだけにする。
# 05〜13ぶんは追跡してしまったので履歴には残っている。
assets/atlas-src/

# レシートOCRの私的テスト画像。購入情報を含むため公開リポジトリへ入れない。
private-test-fixtures/

# macOSが勝手に作るもの
.DS_Store

# ローカル確認用のレビュー成果物とPlaywrightの作業ファイル（公開Pagesへ載せない）
review/
.playwright-mcp/
